import androidx.recyclerview.widget.RecyclerView
import com.example.appratingsoft.databinding.ItemConfiguracionBinding

class ConfiguracionViewHolder(binding: ItemConfiguracionBinding) : RecyclerView.ViewHolder(binding.root) {
    // Puedes agregar métodos o funciones aquí según tus necesidades
    fun bind(configuracionItem: ConfiguracionItem) {
        // Implementa la lógica de vinculación de datos aquí
    }
}
